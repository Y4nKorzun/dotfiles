local colors = {
  orange = "#D77757",
  orange_bright = "#EB9F7F",
  purple = "#AF87FF",
  purple_bright = "#D0B4FF",
  blue = "#93A5FF",
  blue_bright = "#B1B9F9",
  cyan = "#48968C",
  cyan_bright = "#7AB4E8",
  green = "#4EBA65",
  yellow = "#FFC107",
  yellow_bright = "#FFDF39",
  red = "#FF6B80",
  text = "#FFFFFF",
  inactive = "#999999",
  subtle = "#505050",
  surface = "#262626",
  surface_alt = "#373737",
  surface_hover = "#464646",
  surface_bash = "#413C41",
  surface_memory = "#374146",
  selection = "#264F78",
  diff_added = "#225C2B",
  diff_removed = "#7A2936",
}

return vim.tbl_deep_extend("force", require("astrotheme.palettes.astrodark"), {
  syntax = {
    red = colors.red,
    orange = colors.orange,
    yellow = colors.yellow,
    green = colors.green,
    cyan = colors.cyan,
    blue = colors.blue_bright,
    purple = colors.purple,
    text = colors.text,
    comment = colors.inactive,
    mute = colors.subtle,
  },
  ui = {
    red = colors.red,
    orange = colors.orange,
    yellow = colors.yellow,
    green = colors.green,
    cyan = colors.cyan,
    blue = colors.blue_bright,
    purple = colors.purple,
    accent = colors.orange,
    tabline = colors.surface,
    winbar = colors.inactive,
    tool = colors.surface_bash,
    base = colors.surface,
    inactive_base = colors.surface,
    statusline = colors.surface_alt,
    split = colors.subtle,
    float = colors.surface,
    title = colors.orange,
    border = colors.subtle,
    current_line = colors.surface_alt,
    scrollbar = colors.orange,
    selection = colors.selection,
    menu_selection = colors.surface_hover,
    highlight = colors.surface_memory,
    none_text = colors.subtle,
    text = colors.inactive,
    text_active = colors.text,
    text_inactive = colors.subtle,
    text_match = colors.orange,
    prompt = colors.surface_alt,
  },
  term = {
    black = colors.surface,
    bright_black = colors.subtle,
    red = colors.red,
    bright_red = colors.orange_bright,
    green = colors.green,
    bright_green = colors.green,
    yellow = colors.yellow,
    bright_yellow = colors.yellow_bright,
    blue = colors.blue,
    bright_blue = colors.blue_bright,
    purple = colors.purple,
    bright_purple = colors.purple_bright,
    cyan = colors.cyan,
    bright_cyan = colors.cyan_bright,
    white = colors.inactive,
    bright_white = colors.text,
    background = colors.surface,
    foreground = colors.text,
  },
  diff = {
    added = colors.diff_added,
    removed = colors.diff_removed,
    changed = colors.surface_memory,
  },
})
